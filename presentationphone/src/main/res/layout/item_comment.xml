<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="comment"
            type="com.pnam.watchingsocceronline.model.model.Comment" />

        <import type="coil.request.ImageRequest.Builder" />

        <import type="kotlin.Unit" />

        <import type="kotlin.jvm.functions.Function1" />

        <variable
            name="avatar_handle"
            type="Function1&lt;Builder, Unit>" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clickable="true"
        android:focusable="true"
        android:foreground="@drawable/ripple"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/avatar"
            image_builder="@{avatar_handle}"
            url_image="@{comment.user.avatar}"
            android:layout_width="wrap_content"
            android:layout_height="56dp"
            android:adjustViewBounds="true"
            android:background="@android:color/transparent"
            android:clickable="true"
            android:focusable="true"
            android:padding="8dp"
            android:scaleType="fitCenter"
            app:srcCompat="@drawable/ic_home"
            tools:src="@drawable/ic_home" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_margin="8dp"
                    android:backgroundTint="?attr/greyNightDark"
                    android:gravity="start|center"
                    android:text="@{comment.user.firstName}"
                    android:textColor="?attr/blackWhite"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    tools:text="Phạm" />

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_margin="8dp"
                    android:layout_weight="1"
                    android:backgroundTint="?attr/greyNightDark"
                    android:gravity="start|center"
                    android:text="@{comment.user.lastName}"
                    android:textColor="?attr/blackWhite"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    tools:text="Phương Nam" />
            </LinearLayout>

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="8dp"
                android:layout_weight="1"
                android:backgroundTint="?attr/greyNightDark"
                android:gravity="start|center"
                android:text="@{comment.content}"
                android:textColor="?attr/greyAttr"
                tools:text="Comment test" />
        </LinearLayout>
    </LinearLayout>
</layout>